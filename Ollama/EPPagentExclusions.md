# EPP Agent Exclusions

This is to brain dumpan idea to use an LLM agent to fish out vendor recomended EPP exclusions.  Will need to get the agent to pull them out from source but then apply further logic to se if they fit into the requested scenario given by the user.

Maybe apply a different agent / modelfile for each type of situation or even by vendor.


